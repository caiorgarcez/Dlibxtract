# dlibxtract requirements file for anaconda
# USAGE: conda env create -f requirements.yml

name: dlibxtract

channels:
    - conda-forge
    - defaults

dependencies:
    - python=3.6
    - opencv
    - dlib
    - imutils
    - scipy
    